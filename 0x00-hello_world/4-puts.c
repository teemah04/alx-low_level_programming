#include <stdio.h>
/**
 * main:it serves as the entry point of the program
 * Return:By default returns zero if there is no errot otherwise returns non-zero value.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0)
}
