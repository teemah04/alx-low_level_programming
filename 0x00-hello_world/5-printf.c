#include <stdio.h>
/**
 * main:serves as the entry point of the program.
 * return:by default returns zero if there are no errors otherwise prints a non-zero value.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
